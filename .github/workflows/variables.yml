name: Variables

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Print Something
      run: echo $GITHUB_SHA > V1

    - uses: actions/upload-artifact@v3
      with:
        name: Artifacts
        path: |
          V1
